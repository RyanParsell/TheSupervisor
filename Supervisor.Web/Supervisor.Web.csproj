<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Supervisor.Web</RootNamespace>
  </PropertyGroup>

  <!--
    FrameworkReference rather than the Web SDK: this is a library the CLI hosts in-process,
    not a standalone web app with its own entry point.
  -->
  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Supervisor.Core\Supervisor.Core.csproj" />
  </ItemGroup>

</Project>
